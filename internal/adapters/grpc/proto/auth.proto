syntax = "proto3";

package auth.v1;

option go_package = "github.com/vertercloud/auth-service/internal/adapters/grpc/proto;proto";

// AuthService provides gRPC endpoints for inter-service authentication
// API version: v1
service AuthService {
  // ValidateToken validates a JWT token and returns user info
  rpc ValidateToken(ValidateTokenRequest) returns (ValidateTokenResponse);

  // RevokeToken revokes a token
  rpc RevokeToken(RevokeTokenRequest) returns (RevokeTokenResponse);

  // GetUserByID retrieves user information by ID
  rpc GetUserByID(GetUserByIDRequest) returns (GetUserByIDResponse);
}

// ValidateToken messages
message ValidateTokenRequest {
  string token = 1;
}

message ValidateTokenResponse {
  bool valid = 1;
  string user_id = 2;
  string email = 3;
  string username = 4;
  bool active = 5;
  bool email_verified = 6;
  bool two_factor_enabled = 7;
  string error_code = 8;
  string error_message = 9;
}

// RevokeToken messages
message RevokeTokenRequest {
  string token = 1;
}

message RevokeTokenResponse {
  bool success = 1;
  string message = 2;
}

// GetUserByID messages
message GetUserByIDRequest {
  string user_id = 1;
}

message GetUserByIDResponse {
  string id = 1;
  string username = 2;
  string email = 3;
  bool active = 4;
  bool email_verified = 5;
  bool two_factor_enabled = 6;
  string created_at = 7;
}
